package org.example.parkingmanager;

import org.example.parkingslot.ParkingSlot;

import java.util.List;

public class MotorCycleParkManager extends ParkingSlotManager {
    public MotorCycleParkManager(List<ParkingSlot> parkingSlotList) {
        super(parkingSlotList);
    }
}
